<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission for Internet access -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nethmi"
        tools:targetApi="31">

        <!-- Define activities -->
        <activity
            android:name=".CheckoutFragment"
            android:exported="false" />
        <activity
            android:name=".PaymentDoneFragment"
            android:exported="false" />
        <activity
            android:name=".CartFragment"
            android:exported="false" />
        <activity
            android:name=".Login"
            android:exported="false" />
        <activity
            android:name=".Signup"
            android:exported="false" />
        <activity
            android:name=".ProfileFragment"
            android:exported="false" />
        <activity
            android:name=".ShopFragment"
            android:exported="false" />
        <activity
            android:name=".NewsFragment"
            android:exported="false" />
        <activity
            android:name=".HomeFragment"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- Admin Panel Activity -->
        <activity
            android:name=".AdminPanelActivity"
            android:exported="false" />

        <!-- Add Product Activity -->
        <activity
            android:name=".AddProductActivity"
            android:exported="false" />

        <!-- Display Product Activity -->
        <activity
            android:name=".DisplayProductActivity"
            android:exported="false" />

        <!-- Main activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
